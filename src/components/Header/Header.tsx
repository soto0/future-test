import {FC} from 'react';

const Header: FC = () => {
    return (
        <header>
            <div className="container">
                <h1 className="header__logo">Search for books</h1>
            </div>
        </header>
    );
};

export default Header;